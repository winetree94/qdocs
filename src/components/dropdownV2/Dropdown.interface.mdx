import { Meta, Story } from '@storybook/blocks';

import * as DropdownStories from './Dropdown.stories';

<Meta of={DropdownStories} />

# QueueDropdown API

## QueueDropdown Component Interface

```tsx
// Queue에서 간략하게 만들 구조
<QueueDropdown>
  <DropdownMenu.Trigger />

  <DropdownMenu.Content>
    <DropdownMenu.Item />

    <DropdownMenu.Group label="">
      <DropdownMenu.Item />
    </DropdownMenu.Group>

    <DropdownMenu.CheckboxItem indicatorPosition="pre | suf"/>


  </DropdownMenu.Content>
</QueueDropdown>
```


```tsx
// Radix UI 기본 구조
<DropdownMenu.Root>
  <DropdownMenu.Trigger />

  <DropdownMenu.Portal>
    <DropdownMenu.Content>
      <DropdownMenu.Label />
      <DropdownMenu.Item />

      <DropdownMenu.Group>
        <DropdownMenu.Item />
      </DropdownMenu.Group>

      <DropdownMenu.CheckboxItem>
        <DropdownMenu.ItemIndicator />
      </DropdownMenu.CheckboxItem>

      <DropdownMenu.RadioGroup>
        <DropdownMenu.RadioItem>
          <DropdownMenu.ItemIndicator />
        </DropdownMenu.RadioItem>
      </DropdownMenu.RadioGroup>

      <DropdownMenu.Sub>
        <DropdownMenu.SubTrigger />
        <DropdownMenu.Portal>
          <DropdownMenu.SubContent />
        </DropdownMenu.Portal>
      </DropdownMenu.Sub>

      <DropdownMenu.Separator />
      <DropdownMenu.Arrow />
    </DropdownMenu.Content>
  </DropdownMenu.Portal>
</DropdownMenu.Root>
```